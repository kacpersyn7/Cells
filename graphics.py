import matplotlib.pyplot as plt


def show_accesspoints(bitmap):
    plt.plot(iterated, target_f, 'ro')
    plt.show()
    plt.plot(x, y, 'ro')
    plt.axis([0, g.COLS - 1, 0, g.ROWS - 1])
    x, y = np.where(best_bitmap[-1][0] == 1)
    plt.plot(x, y, 'bx'
                   '')
    plt.axis([0, g.COLS - 1, 0, g.ROWS - 1])
    plt.show()

    plt.show()
